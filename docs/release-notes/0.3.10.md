# OpenRamble 0.3.10

## Finishing touches

After a successful first dictation, onboarding now says where the app
lives — in the menu bar at the top of the screen — so the closing window
never reads as a vanished app. The try-out skip is a quiet button instead
of a link, and window activation uses the current macOS API throughout.
