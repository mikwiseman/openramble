<?xml version='1.0' encoding='utf-8'?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
  <channel>
    <title>OpenRamble</title>
    <link>https://mikwiseman.github.io/openramble/appcast.xml</link>
    <description>OpenRamble updates</description>
    <language>en</language>
    <item>
      <title>0.8.3</title>
      <pubDate>Mon, 17 Aug 2026 09:07:10 +0300</pubDate>
      <sparkle:version>25</sparkle:version>
      <sparkle:shortVersionString>0.8.3</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description>&lt;h2&gt;0.8.3&lt;/h2&gt;&lt;p&gt;## Setup no longer waits for something that is not happening&lt;/p&gt;&lt;p&gt;On a fresh install, setup could stop for good: the model card said &amp;quot;Model ready&amp;quot;,&lt;/p&gt;&lt;p&gt;both permissions were granted, and Continue stayed grey under the words &amp;quot;Getting&lt;/p&gt;&lt;p&gt;the model ready…&amp;quot;. Only quitting and relaunching cleared it.&lt;/p&gt;&lt;p&gt;Nothing was preparing the model at that moment. When the Mac runs low on memory —&lt;/p&gt;&lt;p&gt;likely right after a 586 MB download while the model is being compiled for this&lt;/p&gt;&lt;p&gt;particular Mac — the app lets the recogniser rest until it is next needed. That is&lt;/p&gt;&lt;p&gt;the right thing to do for memory. But the setup screen was waiting for a loaded&lt;/p&gt;&lt;p&gt;recogniser, and the only things that wake one are pressing the dictation key or&lt;/p&gt;&lt;p&gt;switching back to the app, neither of which happens while a person sits on the&lt;/p&gt;&lt;p&gt;setup screen. Two sensible rules held each other.&lt;/p&gt;&lt;p&gt;The previous two releases each closed one way *into* that state, and it came back&lt;/p&gt;&lt;p&gt;through another door. This release removes the requirement instead: **no part of&lt;/p&gt;&lt;p&gt;the first run asks for a loaded recogniser any more.** Setup needs the permissions&lt;/p&gt;&lt;p&gt;and the model files, nothing else. The recogniser loads when it is actually needed,&lt;/p&gt;&lt;p&gt;and dictation already waits for a cold one and inserts the text when it arrives —&lt;/p&gt;&lt;p&gt;so a condition that cannot deadlock replaces one that had to be rescued.&lt;/p&gt;&lt;p&gt;## One answer per question&lt;/p&gt;&lt;p&gt;The menu, the setup card and the try-out step used to describe the model&amp;#x27;s state&lt;/p&gt;&lt;p&gt;separately, and could disagree — which is how one screen came to say &amp;quot;Model ready&amp;quot;&lt;/p&gt;&lt;p&gt;and &amp;quot;Getting the model ready…&amp;quot; at the same instant. They now all read the same&lt;/p&gt;&lt;p&gt;sentence from the same place, so they cannot contradict each other.&lt;/p&gt;&lt;p&gt;The try-out step also shows the first compile while it happens, instead of leaving&lt;/p&gt;&lt;p&gt;a person to wonder why their first attempt is slow.&lt;/p&gt;&lt;p&gt;## Under the hood&lt;/p&gt;&lt;p&gt;New tests hold the setup screen to a rule it can be measured against: at every&lt;/p&gt;&lt;p&gt;moment it either lets a person move on, or is blocked by work genuinely in motion&lt;/p&gt;&lt;p&gt;and says truthfully what that work is. A screen waiting for work nobody performs&lt;/p&gt;&lt;p&gt;now fails the suite, and so does a card that contradicts its own footer.&lt;/p&gt;</description>
      <enclosure url="https://github.com/mikwiseman/openramble/releases/download/v0.8.3/OpenRamble-0.8.3.dmg" length="7487821" type="application/octet-stream" sparkle:edSignature="vY53qTuE9lMC20ozNs9XGbozPeX8aTI4m1FNRA12Vc6N9k4nF8wigIQcdh5fqULsPdMQ2nEgGcrByMM7jQsvBg==" />
    </item>
  </channel>
</rss>
