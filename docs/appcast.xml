<?xml version='1.0' encoding='utf-8'?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
  <channel>
    <title>OpenRamble</title>
    <link>https://mikwiseman.github.io/openramble/appcast.xml</link>
    <description>OpenRamble updates</description>
    <language>en</language>
    <item>
      <title>0.14.0</title>
      <pubDate>Wed, 19 Aug 2026 17:02:21 +0300</pubDate>
      <sparkle:version>32</sparkle:version>
      <sparkle:shortVersionString>0.14.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description>&lt;h2&gt;OpenRamble 0.14.0&lt;/h2&gt;&lt;p&gt;Dictation sometimes took several seconds to come back. This release fixes what&lt;/p&gt;&lt;p&gt;was actually causing it, and it was not the recogniser.&lt;/p&gt;&lt;p&gt;## The stall&lt;/p&gt;&lt;p&gt;Recognition was usually fast and occasionally many seconds slow, with no&lt;/p&gt;&lt;p&gt;pattern anyone could see. It was not the model warming up, and it was not the&lt;/p&gt;&lt;p&gt;model being reloaded — both were checked and neither matched the slow takes.&lt;/p&gt;&lt;p&gt;Eight hours of this Mac&amp;#x27;s own logs settled it. Across 155 dictations the&lt;/p&gt;&lt;p&gt;inference call itself never once exceeded 1.07 seconds. Every bit of the delay&lt;/p&gt;&lt;p&gt;sat *outside* the recogniser, in the gap between the microphone stopping and&lt;/p&gt;&lt;p&gt;the text appearing.&lt;/p&gt;&lt;p&gt;The cause was the microphone teardown. Stopping an audio engine can block&lt;/p&gt;&lt;p&gt;inside Apple&amp;#x27;s own framework — this app has known that for a long time, and&lt;/p&gt;&lt;p&gt;says so in a comment right above the code. What it did with that knowledge was&lt;/p&gt;&lt;p&gt;run the blocking call on Swift&amp;#x27;s cooperative thread pool, which has one thread&lt;/p&gt;&lt;p&gt;per processor core and which must never be blocked, because a blocked thread&lt;/p&gt;&lt;p&gt;there is not given back. The recognition waiting to finish was queued on that&lt;/p&gt;&lt;p&gt;same pool. So the end of one dictation could hold the result of it behind a&lt;/p&gt;&lt;p&gt;framework call that had gone quiet.&lt;/p&gt;&lt;p&gt;The evidence for that is visible in the logs: during every stall the app&amp;#x27;s&lt;/p&gt;&lt;p&gt;main thread kept running normally, once-a-second background checks stopped&lt;/p&gt;&lt;p&gt;arriving and then landed in a single burst of thirteen at once — thirteen&lt;/p&gt;&lt;p&gt;pieces of work that had been queued the whole time and never given a thread.&lt;/p&gt;&lt;p&gt;Audio teardown now runs on a thread of its own, which is allowed to block. The&lt;/p&gt;&lt;p&gt;same defect was fixed in the once-a-second permission check, which was parking&lt;/p&gt;&lt;p&gt;a pool thread on another process answering, 3,600 times an hour.&lt;/p&gt;&lt;p&gt;## What did not change&lt;/p&gt;&lt;p&gt;Nothing was removed on suspicion. The machinery that bounds how many abandoned&lt;/p&gt;&lt;p&gt;audio engines can exist at once stays exactly as it was: it carries a safety&lt;/p&gt;&lt;p&gt;property, and the tempting simplification would have quietly given that up.&lt;/p&gt;&lt;p&gt;## Choose your microphone&lt;/p&gt;&lt;p&gt;Settings now lists the inputs this Mac can hear through. A microphone you chose&lt;/p&gt;&lt;p&gt;that is later unplugged does not silently become a different one — dictation&lt;/p&gt;&lt;p&gt;continues through the system default and Settings says so, because otherwise&lt;/p&gt;&lt;p&gt;you would find out from the transcript.&lt;/p&gt;</description>
      <enclosure url="https://github.com/mikwiseman/openramble/releases/download/v0.14.0/OpenRamble-0.14.0.dmg" length="5785487" type="application/octet-stream" sparkle:edSignature="ZZX8QC8bRrje6e+GbRQQpUly7lz8gSuq2Z5JPhLdi5px1PYPik4eYSMhfgIkt3GTHKHZOi9x1NsYTQq4EK+sCQ==" />
    </item>
  </channel>
</rss>
