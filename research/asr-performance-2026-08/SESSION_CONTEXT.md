# Session context at handoff

The active task changed from open-ended research to durable transfer: save every
useful source change, result, failure, instruction, and constraint in Git so a
new agent can continue without the original conversation.

The product branch contains four tested performance commits. Later experiments
mostly used isolated `/private/tmp` worktrees. The archive inventories every root,
copies reviewable content, and captures every dirty Git diff.

The strongest unresolved result is exact long-form closed-window caching. The
key negative conclusion is that the current full-context short path is already
tens of milliseconds, so a universal 10× short win requires a different model
or canonical endpoint semantics. The only current source-only next candidate is
PengChengStarling streaming Zipformer on CPU, pending an initial-language-token
contract and a sealed tiny smoke.

The installed OpenRamble app was left stopped intentionally. The next agent must
coordinate model access, avoid broad kills, preserve privacy/license exclusions,
and relaunch only at the true end.

The complete actionable continuation is in `CONTINUATION_PROMPT.md`.
